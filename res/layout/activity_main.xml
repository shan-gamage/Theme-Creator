<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity"
    android:id="@+id/mainActivityRelativeLayout" >

    <TextView
        android:id="@+id/sampleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_world" />

    <Button
        android:id="@+id/btnTheme"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/itemIconTextAwesome"
        android:layout_marginLeft="22dp"
        android:layout_marginTop="23dp"
        android:text="Show My Themes" />

    <com.shana.android.themeCreater.icomoon.IcoMoonTextView
        android:id="@+id/itemIconTextAwesome"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_alignLeft="@+id/sampleTextView"
        android:layout_below="@+id/sampleTextView"
        android:layout_margin="5dp"
        android:layout_marginLeft="43dp"
        android:layout_marginTop="71dp"
        android:gravity="center"
        android:padding="2dp"
        android:textAlignment="center" />

</RelativeLayout>
